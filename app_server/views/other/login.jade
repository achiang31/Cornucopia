extends ../layout

block content
    .row
        .col-lg-12.col-md-12.col-sm-12.col-xs-12
        .login-page
            h1(align='center') Cornucopia
            .form
                form.login-form
                    input(type='text', placeholder='username', id='user')
                    input(type='password', placeholder='password', id='pass')
                    a.btn.btn-default(id = 'login') Login

                    //button(onclick="window.location.href='/users/KevinTian/recipes'") login
                    p.message
                        | Not registered?
                        a(href='/register')  Create an account

    //h1= title
    //p #{username}
    //p #{password}